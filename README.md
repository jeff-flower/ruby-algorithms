# About

Each folder is a separate Ruby project and git repo

To create a new project/folder

TODO: write a script for this

0. `mkdir <name of new project>`
1. `git init`
2. `mkdir lib` && `touch lib/<name of file.rb>`
3. `bundle init`
4. `bundle add rubocop rubocop-rspec pry-byebug rspec`
5. `bundle exec rspec --init`
6. Write a failing test and commit
